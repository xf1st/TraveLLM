import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; import dotenv from 'dotenv'; dotenv.config({path: '.env.local'}); const file = 'd:/sites/TraveLLM/trip-3cb0d8ea.json'; const data = JSON.parse(fs.readFileSync(file, 'utf8')); const img = 'https://upload.wikimedia.org/wikipedia/commons/b/b3/Belgrade_Montage_2022.jpg'; data.cover_image = img; data.coverImage = img; fs.writeFileSync(file, JSON.stringify(data, null, 2)); console.log('File updated.'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); supabase.from('trips').update({ cover_image: img }).eq('id', data.id).then(res => console.log('DB string:', res));
